import { {{pascalCase name}} } from "@hikoui-beta/react";
import type { {{pascalCase name}}Props } from "@hikoui-beta/react";
import type { Meta, StoryObj } from "@storybook/react-vite";
import type { ComponentType } from "react";

const meta: Meta<{{pascalCase name}}Props> = {
  title: "Component/{{pascalCase name}}",
  component: {{pascalCase name}} as ComponentType<{{pascalCase name}}Props>,
  parameters: {
    layout: "centered",
  },
  tags: ["autodocs"],
  argTypes: {
    children: {
      control: "text",
      description: "The text to display",
    },
    className: {
      control: "text",
      description: "Additional CSS classes to apply",
    },
    as: {
      description: "The element type to render as",
      control: "select",
      options: [],
      table: {
        type: {
          summary: "Element",
        },
        defaultValue: {
          summary: "Element",
        },
      },
    },
  },
  args: {},
};

export default meta;

type Story = StoryObj<typeof meta>;

export const Primary: Story = {
  args: {},
};
