import { cva } from "class-variance-authority";
import type { VariantProps } from "class-variance-authority";

const {{kebabCase name}}Variants = cva("{{kebabCase name}}", {
  variants: {},
  compoundVariants: [],
  defaultVariants: {},
});

export type {{pascalCase name}}VariantProps = VariantProps<typeof {{kebabCase name}}Variants>;

export default {{kebabCase name}}Variants;